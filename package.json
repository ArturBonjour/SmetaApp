{
  "name": "smeta-app",
  "private": true,
  "scripts": {
    "dev": "bash start-dev.sh",
    "setup": "cd backend && npm install && node_modules/.bin/prisma db push && node_modules/.bin/ts-node prisma/seed.ts && cd ../frontend && npm install",
    "build": "cd backend && npm run build && cd ../frontend && npm run build"
  }
}
